# 凭据 / 密钥 —— 绝不提交
kuaimai_settings.json
.env
aliyun/
lego/
*.pem
*.key
*.crt
*.pfx
*.jks
keystore*

# 运行时数据 / 缓存 / 日志
kuaimai_*.json
*.db
*.db-wal
*.db-shm
*.log

# 打包产物
build/
dist/
*.spec
*.exe
*.apk
*.zip

# 编辑器 / 系统
.vscode/
.idea/
__pycache__/
*.pyc
Thumbs.db
desktop.ini
kuaimai_users.json

# 运行时产物（不该进版本库）
printed_memory.json
print_progress.json
*.new-empty
*.bak-*
_selftest_*.out.txt
# 打包中间产物
_build_*/

# 打包中间产物 / 第三方二进制（不入库）
packaging/staging/
packaging/out/
packaging/_work/
packaging/frp/
packaging/*.log
https/km_https.exe# 但打包配置要入库（spec 决定 hiddenimports，缺了打出来的包点「打单」会 ModuleNotFoundError）
!packaging/*.spec